[package]
name = "elementium-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared types for Elementium (no heavy deps)"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
